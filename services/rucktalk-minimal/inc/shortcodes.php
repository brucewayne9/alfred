<?php
/**
 * Shortcodes — rucktalk-minimal.
 *
 * Registers:
 *   [rt_signup placement="hero|inline|footer|popup|training-free"]
 *     — Brevo double-opt-in email-capture form (Task 11)
 *   [rt_ecosystem_strip]
 *     — Sister-brand wordmark row (Task 11)
 *   [rt_pillars]
 *     — Five-pillar grid with daily-rotating "Today's take" snippets (Task 13b)
 */

if ( ! defined( 'ABSPATH' ) ) {
    exit;
}

// Tasks 11 + 13b fill these in.
