---
phase: 04-google-ads-budget-control
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - core/tools/definitions.py
  - core/tools/registry.py
  - TOOLS.md
autonomous: true
requirements:
  - GADS-01
  - GADS-02
  - GADS-03

must_haves:
  truths:
    - "gads_update_campaign_budget tool is callable by the LLM and routes to update_campaign_budget()"
    - "gads_pause_ad_group and gads_enable_ad_group tools are callable and route to set_ad_group_status()"
    - "gads_set_campaign_status tool description includes confirmation threshold and verification behavior"
    - "Google Ads tools appear in the tool list when google ads keywords are in the query"
    - "TOOLS.md section 6 includes budget, pause-group, and enable-group commands"
  artifacts:
    - path: "core/tools/definitions.py"
      provides: "Tool decorators for gads_update_campaign_budget, gads_pause_ad_group, gads_enable_ad_group"
      contains: "gads_update_campaign_budget"
    - path: "core/tools/registry.py"
      provides: "Tool category registration for new Google Ads write tools"
      contains: "gads_update_campaign_budget"
    - path: "TOOLS.md"
      provides: "Updated Google Ads section with write commands"
      contains: "budget"
  key_links:
    - from: "core/tools/definitions.py:gads_update_campaign_budget"
      to: "integrations/google_ads/client.py:update_campaign_budget"
      via: "lazy import in tool function"
      pattern: "from integrations.google_ads.client import update_campaign_budget"
    - from: "core/tools/definitions.py:gads_pause_ad_group"
      to: "integrations/google_ads/client.py:set_ad_group_status"
      via: "lazy import with status='PAUSED'"
      pattern: "set_ad_group_status.*PAUSED"
    - from: "core/tools/registry.py"
      to: "core/tools/definitions.py"
      via: "TOOL_CATEGORIES google_ads list"
      pattern: "gads_update_campaign_budget.*gads_pause_ad_group.*gads_enable_ad_group"
---

<objective>
Wire the new Google Ads write functions into the tool system and update TOOLS.md for Alfred Claw.

Purpose: Make the backend functions callable by the LLM, with confirmation threshold guidance in tool descriptions, and document the new commands for Alfred Claw on Server 101.
Output: Registered tools visible to the LLM, updated TOOLS.md with budget and ad group commands.
</objective>

<execution_context>
@/home/aialfred/.claude/get-shit-done/workflows/execute-plan.md
@/home/aialfred/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-google-ads-budget-control/04-RESEARCH.md
@.planning/phases/04-google-ads-budget-control/04-01-SUMMARY.md
@core/tools/definitions.py
@core/tools/registry.py
@TOOLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register new Google Ads write tools in definitions.py and registry.py</name>
  <files>core/tools/definitions.py, core/tools/registry.py</files>
  <action>
**A. Add three new tool definitions in `core/tools/definitions.py`** -- insert after the existing `gads_set_campaign_status` tool block (before the `# ==================== GOOGLE ANALYTICS ====================` section):

1. **`gads_update_campaign_budget`:**
   - Description must include confirmation threshold per locked decision: "Update a Google Ads campaign's daily budget. CONFIRMATION RULES: If the new budget exceeds $100/day OR is more than 2x the current budget, ask Mike to confirm before calling this tool. Always show before/after comparison after the update. If the budget is shared across campaigns, list all affected campaigns and confirm before applying."
   - Parameters: `campaign_id` (string, required), `new_daily_budget` (number, required -- "New daily budget in dollars, e.g. 50.00"), `customer_id` (string, optional)
   - Function body: lazy import `from integrations.google_ads.client import update_campaign_budget` and call it

2. **`gads_pause_ad_group`:**
   - Description: "Pause a Google Ads ad group. For bulk operations (pausing all ad groups in a campaign), list affected ad groups and confirm with Mike before calling. Warn if this is the last active ad group in the campaign. Always verify the status change after applying."
   - Parameters: `ad_group_id` (string, required), `customer_id` (string, optional)
   - Function body: lazy import `from integrations.google_ads.client import set_ad_group_status` and call with `status="PAUSED"`

3. **`gads_enable_ad_group`:**
   - Description: "Enable (unpause) a Google Ads ad group. Always verify the status change after applying."
   - Parameters: `ad_group_id` (string, required), `customer_id` (string, optional)
   - Function body: lazy import `from integrations.google_ads.client import set_ad_group_status` and call with `status="ENABLED"`

**B. Update `gads_set_campaign_status` description** to include confirmation guidance: "Enable or pause a Google Ads campaign. Always show before/after status. For significant changes, confirm with Mike first. Supports multiple accounts."

**C. Add new tool names to `TOOL_CATEGORIES["google_ads"]` in `core/tools/registry.py`:**

Add `"gads_update_campaign_budget"`, `"gads_pause_ad_group"`, `"gads_enable_ad_group"` to the existing `google_ads` list.

**D. Add keyword triggers** to `CATEGORY_KEYWORDS["google_ads"]` in registry.py:

Add: `"budget"`, `"daily budget"`, `"pause ad group"`, `"enable ad group"`, `"unpause"` to the existing keyword list. Note: `"budget"` may also match meta_ads -- that is fine, both categories will be included for budget queries which is the correct behavior.
  </action>
  <verify>
- `python3 -c "from core.tools.definitions import gads_update_campaign_budget, gads_pause_ad_group, gads_enable_ad_group; print('OK')"` prints OK
- `grep -c "gads_update_campaign_budget" core/tools/definitions.py` returns at least 2 (decorator name + function def)
- `grep -c "gads_pause_ad_group" core/tools/definitions.py` returns at least 2
- `grep -c "gads_enable_ad_group" core/tools/definitions.py` returns at least 2
- `grep "gads_update_campaign_budget" core/tools/registry.py` shows it in the google_ads category list
- `python3 -c "from core.tools.registry import get_relevant_tools; tools = get_relevant_tools('update google ads budget'); print('gads_update_campaign_budget' in tools)"` prints True
  </verify>
  <done>
- gads_update_campaign_budget, gads_pause_ad_group, gads_enable_ad_group registered as LLM-callable tools
- gads_set_campaign_status description updated with confirmation guidance
- All new tools in TOOL_CATEGORIES["google_ads"]
- Budget/ad group keywords added to CATEGORY_KEYWORDS["google_ads"]
  </done>
</task>

<task type="auto">
  <name>Task 2: Update TOOLS.md Google Ads section with new write commands</name>
  <files>TOOLS.md</files>
  <action>
Update section 6 (Google Ads) in TOOLS.md to add the new write commands. Use the same terse format as existing commands in the file. The TOOLS.md file has a ~20,000 char limit (currently ~19,766 per MEMORY.md), so be very concise.

Add these commands to the Google Ads section, after existing read commands:

```
budget <campaign_id> <daily_dollars> - update campaign daily budget (dollars, e.g. 50.00). Shows before/after. Warns if shared budget.
pause-group <ad_group_id> - pause an ad group. Verifies status change.
enable-group <ad_group_id> - enable an ad group. Verifies status change.
```

If the file would exceed ~20,000 chars after the addition, trim whitespace or compress other sections slightly. Measure with `wc -c TOOLS.md` before and after.

Do NOT add audit log documentation to TOOLS.md -- the audit log is internal infrastructure, not a user-facing command.
  </action>
  <verify>
- `grep "budget" TOOLS.md` shows the new budget command
- `grep "pause-group" TOOLS.md` shows the pause command
- `grep "enable-group" TOOLS.md` shows the enable command
- `wc -c TOOLS.md` shows file is under 20,000 characters
  </verify>
  <done>TOOLS.md Google Ads section includes budget, pause-group, and enable-group commands, file stays under 20,000 char limit</done>
</task>

</tasks>

<verification>
1. `python3 -c "from core.tools.registry import get_tools; names = [t['name'] for t in get_tools()]; assert 'gads_update_campaign_budget' in names; assert 'gads_pause_ad_group' in names; assert 'gads_enable_ad_group' in names; print('All tools registered')"` succeeds
2. `python3 -c "from core.tools.registry import get_relevant_tools; t = get_relevant_tools('change google ads budget for campaign'); print([x for x in t if x.startswith('gads')])"` shows all gads tools including new ones
3. `wc -c TOOLS.md` shows under 20,000 chars
4. `grep -c "budget\|pause-group\|enable-group" TOOLS.md` returns 3
</verification>

<success_criteria>
- Three new tool definitions registered and importable
- gads_set_campaign_status description updated with confirmation behavior
- TOOL_CATEGORIES and CATEGORY_KEYWORDS updated for keyword routing
- TOOLS.md updated with concise write command documentation under char limit
- LLM can discover Google Ads write tools via keyword matching
</success_criteria>

<output>
After completion, create `.planning/phases/04-google-ads-budget-control/04-02-SUMMARY.md`
</output>
