---
phase: 08-recovery-alerting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/backup/backup_alerting.py
  - scripts/backup/daily_backup.py
  - scripts/backup/weekly_backup.py
  - scripts/backup/validate_backups.py
autonomous: true
requirements: [RECOV-01, RECOV-03]

must_haves:
  truths:
    - "When daily_backup.py or weekly_backup.py has any server failure, Mike receives a Telegram message identifying which server failed"
    - "A validation script checks the latest daily and weekly backups in Drive and reports present/non-empty per server"
    - "Validation results are written to data/backup_status.json for downstream consumption"
    - "Validation runs automatically via cron after the backup window"
  artifacts:
    - path: "scripts/backup/backup_alerting.py"
      provides: "Telegram notification for backup failures"
      contains: "send_backup_alert"
    - path: "scripts/backup/validate_backups.py"
      provides: "Backup integrity checker and status reporter"
      contains: "validate_all_servers"
  key_links:
    - from: "scripts/backup/daily_backup.py"
      to: "scripts/backup/backup_alerting.py"
      via: "import send_backup_alert, called in main() when failures exist"
      pattern: "send_backup_alert"
    - from: "scripts/backup/weekly_backup.py"
      to: "scripts/backup/backup_alerting.py"
      via: "import send_backup_alert, called in main() when failures exist"
      pattern: "send_backup_alert"
    - from: "scripts/backup/validate_backups.py"
      to: "scripts/backup/drive_manager.py"
      via: "import list_date_folders to check latest backup dates and file counts"
      pattern: "list_date_folders"
    - from: "scripts/backup/validate_backups.py"
      to: "data/backup_status.json"
      via: "json.dump writes validation results"
      pattern: "backup_status\\.json"
---

<objective>
Add Telegram failure alerts to both backup scripts and create a validation script that verifies backup integrity.

Purpose: Mike must know immediately when a backup fails, and a machine-readable status file must exist for Alfred to answer "did last night's backup succeed?"
Output: backup_alerting.py (Telegram notifier), modified daily/weekly scripts with alert calls, validate_backups.py (Drive checker + status writer), cron entry for validation
</objective>

<execution_context>
@/home/aialfred/.claude/get-shit-done/workflows/execute-plan.md
@/home/aialfred/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-backup-system/07-01-SUMMARY.md
@.planning/phases/07-backup-system/07-02-SUMMARY.md
@.planning/phases/07-backup-system/07-03-SUMMARY.md
@scripts/backup/daily_backup.py
@scripts/backup/weekly_backup.py
@scripts/backup/drive_manager.py
@scripts/backup/backup_utils.py
@scripts/alfred_claw_monitor.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create backup alerting module and wire into daily/weekly scripts</name>
  <files>scripts/backup/backup_alerting.py, scripts/backup/daily_backup.py, scripts/backup/weekly_backup.py</files>
  <action>
Create `scripts/backup/backup_alerting.py` with a `send_backup_alert(backup_type: str, results: list[dict]) -> bool` function that:
1. Filters results for failed servers (where `success == False`)
2. If no failures, returns False (no alert sent)
3. Builds a message like: "Backup Alert: daily backup had 2/7 failures\n- alfred-claw: Drive upload failed: ...\n- lonewolf: SSH timeout"
4. Sends via SSH to Alfred Claw's Telegram: `ssh claw 'openclaw message send --channel telegram --target 7582976864 --message "..."'` — use subprocess with timeout=30
5. Wraps the entire send in try/except so alert failure never crashes the backup script
6. Logs the alert attempt (success or failure) via logging module
7. Use the `claw` SSH alias (defined in ~/.ssh/config from Phase 6) — NOT the raw `ssh -p 2222 brucewayne9@...` pattern

Modify `scripts/backup/daily_backup.py` main() function:
- After the results summary section (after the `logger.info("=" * 60)` block at the end), add:
  ```python
  from scripts.backup.backup_alerting import send_backup_alert
  if failed:
      send_backup_alert("daily", results)
  ```

Modify `scripts/backup/weekly_backup.py` main() function similarly:
- After its summary block, add the same pattern with backup_type="weekly"

Keep the import inside the if-block or at module top — either works, but be consistent with the existing import style in these files (module-level imports preferred).
  </action>
  <verify>
1. `python3 -c "from scripts.backup.backup_alerting import send_backup_alert; print('OK')"` succeeds
2. `grep -n 'send_backup_alert' scripts/backup/daily_backup.py` shows the call in main()
3. `grep -n 'send_backup_alert' scripts/backup/weekly_backup.py` shows the call in main()
4. `python3 -c "import ast; ast.parse(open('scripts/backup/daily_backup.py').read()); print('syntax ok')"` passes
5. `python3 -c "import ast; ast.parse(open('scripts/backup/weekly_backup.py').read()); print('syntax ok')"` passes
  </verify>
  <done>
- backup_alerting.py exists with send_backup_alert() that sends Telegram via SSH to claw
- daily_backup.py calls send_backup_alert("daily", results) when any server fails
- weekly_backup.py calls send_backup_alert("weekly", results) when any server fails
- Alert failure never crashes the backup script (try/except wrap)
  </done>
</task>

<task type="auto">
  <name>Task 2: Create backup validation script with Drive checks and cron</name>
  <files>scripts/backup/validate_backups.py</files>
  <action>
Create `scripts/backup/validate_backups.py` — a standalone executable script that:

1. Imports from `scripts.backup.drive_manager` (list_date_folders) and `scripts.backup.backup_utils` (SERVERS)
2. Has `validate_server(server_name: str, backup_type: str, expected_within_hours: int) -> dict` that:
   - Calls `list_date_folders(server_name, backup_type)` to get all date folders
   - Checks if the latest folder date is within `expected_within_hours` of now (daily=26h to cover timezone drift, weekly=170h for ~7 days)
   - Lists files in the latest date folder using `list_files(folder_id=latest_folder_id)` from the Drive client
   - Reports: `{server, backup_type, latest_date, age_hours, file_count, total_size_bytes, status}` where status is "ok", "stale" (too old), "empty" (no files), or "missing" (no folders at all)
3. Has `validate_all_servers() -> dict` that:
   - Loops over all 7 servers from SERVERS list
   - Checks both "daily" and "weekly" for each
   - Returns `{timestamp, servers: {server_name: {daily: {...}, weekly: {...}}}, summary: {total_checks, ok, stale, empty, missing}}`
4. Has `main()` that:
   - Runs validate_all_servers()
   - Writes result to `data/backup_status.json` (atomic write: write to .tmp then os.rename)
   - Logs a summary line
   - If any status is not "ok", calls `send_backup_alert("validation", results_list)` from backup_alerting.py where results_list is formatted to show which servers have issues
   - Returns exit code 0 if all ok, 1 if any issues

Install cron: `0 5 * * * /home/aialfred/alfred/venv/bin/python3 /home/aialfred/alfred/scripts/backup/validate_backups.py >> /home/aialfred/alfred/data/backup_validate.log 2>&1`
(5 AM daily — gives 3 hours after the 2 AM backup window for all uploads to complete)

Configure logging matching the daily_backup.py pattern (stdout + file handler to data/backup_validate.log).
  </action>
  <verify>
1. `python3 -c "from scripts.backup.validate_backups import validate_all_servers; print('import OK')"` succeeds
2. `python3 -c "import ast; ast.parse(open('scripts/backup/validate_backups.py').read()); print('syntax ok')"` passes
3. `crontab -l | grep validate_backups` shows the 5 AM daily entry
  </verify>
  <done>
- validate_backups.py exists and can check Drive for latest backups per server
- Writes data/backup_status.json with per-server daily/weekly status
- Sends Telegram alert if any backup is stale, empty, or missing
- Cron installed at 5 AM daily to run after the backup window
  </done>
</task>

</tasks>

<verification>
1. Both backup scripts (daily + weekly) have alert calls wired in
2. Validation script can import and parse without errors
3. Cron entry for validation exists at 5 AM
4. backup_alerting.py can be imported independently
5. No existing functionality broken (daily/weekly scripts still parse cleanly)
</verification>

<success_criteria>
- Backup failures trigger Telegram alerts to Mike (chat ID 7582976864)
- Validation script checks all 7 servers' daily and weekly backups on Drive
- Status JSON written to data/backup_status.json for API consumption
- All 3 new files + 2 modified files parse without syntax errors
</success_criteria>

<output>
After completion, create `.planning/phases/08-recovery-alerting/08-01-SUMMARY.md`
</output>
