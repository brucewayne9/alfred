---
phase: 02-alfred-claw-config-fixes
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.openclaw/workspace/USER.md
  - ~/.openclaw/workspace/HEARTBEAT.md
autonomous: true
gap_closure: true
requirements: [CLAW-02, CLAW-03]

must_haves:
  truths:
    - "USER.md loads without truncation WARN in OpenClaw bootstrap logs"
    - "HEARTBEAT.md loads without truncation WARN in OpenClaw heartbeat logs"
    - "USER.md retains Mike's identity, family names/birthdays, business context, and Queue/Escalation section"
    - "HEARTBEAT.md retains at least one actionable checklist item and HEARTBEAT_OK instruction"
  artifacts:
    - path: "~/.openclaw/workspace/USER.md"
      provides: "Mike's profile under 2,520 chars"
      contains: "Ground Rush"
    - path: "~/.openclaw/workspace/HEARTBEAT.md"
      provides: "Heartbeat micro-checklist under 149 chars"
      contains: "HEARTBEAT_OK"
  key_links:
    - from: "~/.openclaw/workspace/USER.md"
      to: "OpenClaw bootstrap injection"
      via: "workspace file loader"
      pattern: "wc -c.*USER.md.*under 2520"
    - from: "~/.openclaw/workspace/HEARTBEAT.md"
      to: "OpenClaw heartbeat framework"
      via: "heartbeat checklist injection"
      pattern: "wc -c.*HEARTBEAT.md.*under 149"
---

<objective>
Further trim USER.md (3,798 -> under 2,520 chars) and HEARTBEAT.md (231 -> under 149 chars) to match the actual OpenClaw runtime injection limits discovered during verification.

Purpose: Plan 02-01 trimmed these files to the wrong limits (3,955 and 293 respectively). Verification revealed the actual runtime limits are 2,520 chars for USER.md and 149 chars for HEARTBEAT.md. Both files still emit truncation WARNs on every bootstrap/heartbeat.

Output: Both files under their true limits, no more truncation warnings.
</objective>

<execution_context>
@/home/aialfred/.claude/get-shit-done/workflows/execute-plan.md
@/home/aialfred/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-alfred-claw-config-fixes/02-01-SUMMARY.md
@.planning/phases/02-alfred-claw-config-fixes/02-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trim USER.md from 3,798 to under 2,520 chars and HEARTBEAT.md from 231 to under 149 chars</name>
  <files>~/.openclaw/workspace/USER.md, ~/.openclaw/workspace/HEARTBEAT.md</files>
  <action>
SSH to Server 101 (`ssh -p 2222 brucewayne9@75.43.156.101`).

**USER.md — must cut ~1,278+ chars (from 3,798 to under 2,520):**

1. Read current USER.md: `cat ~/.openclaw/workspace/USER.md | wc -c` to confirm starting size.
2. Back up: `cp ~/.openclaw/workspace/USER.md ~/.openclaw/workspace/USER.md.bak-gap-20260220`
3. Apply these cuts (per 02-01-SUMMARY.md, the file already has: identity, business, digital properties, communication prefs, clients, infrastructure, family, key dates, decision making, Queue/Escalation):
   - **Infrastructure section**: Compress from multi-line IP:role format to a single comma-separated line. E.g., "Servers: 101(Claw), 104(HA), 105(Labs), 117(Dokploy/CRM), 127(DNS)"
   - **Digital properties**: If still multi-line URLs, compress to comma-separated domain names only (no full URLs)
   - **Communication preferences**: Cut to 2-3 essential bullets max (remove any that duplicate SOUL.md personality)
   - **Decision making style section**: Cut entirely or compress to 1 sentence — this is personality info that belongs in SOUL.md
   - **Current clients detail**: Compress to 1-2 lines (name + what they do, no sub-details)
   - **Family section**: Keep names and birthdays ONLY. Cut all other family detail. Format: "Wife: Sarah (Jan 1). Kids: Rowan (Oct 21), Rex (Oct 8), Brandon (Dec 28), Hillary (Dec 5)"
   - **Key dates table**: If a separate section, merge into family line above
   - Keep Queue/Escalation CRITICAL section verbatim (operational requirement)
   - Keep core identity (name, role, company) verbatim
4. Target: 2,400 chars (120 chars margin below 2,520 limit)
5. Verify: `wc -c ~/.openclaw/workspace/USER.md` — must show under 2,520
6. Verify key content: `grep -c -E "Ground Rush|Sarah|Rowan|Rex|QUEUE|ESCALAT" ~/.openclaw/workspace/USER.md` — must be >= 6

**HEARTBEAT.md — must cut ~82+ chars (from 231 to under 149):**

1. Read current content: `cat ~/.openclaw/workspace/HEARTBEAT.md`
2. Back up: `cp ~/.openclaw/workspace/HEARTBEAT.md ~/.openclaw/workspace/HEARTBEAT.md.bak-gap-20260220`
3. Current content is approximately (from 02-01-SUMMARY.md): 5-line micro-checklist with header, inbox check, grep command, birthday check, monitor line, and HEARTBEAT_OK instruction. At 231 chars this needs to lose ~82 chars.
4. Rewrite to ultra-compact format (target: ~140 chars):
   ```
   # Heartbeat
   - Inboxes: alfred@, mjohnson@
   - grep -E -i "PENDING|ESCALATED" ~/.openclaw/workspace/QUEUE.md
   - Birthdays within 7d
   Reply HEARTBEAT_OK if clear.
   ```
   If still over 149 chars, cut further:
   - Remove the `# Heartbeat` header if needed (but keep at least one substantive line — empty HEARTBEAT.md skips heartbeat entirely per OpenClaw docs)
   - Shorten grep to just `grep -E -i "PENDING|ESCALATED" QUEUE.md` (relative path — shorter, and heartbeat may run from workspace dir)
   - Combine lines: "Check inboxes, QUEUE.md, birthdays within 7d"

   CRITICAL: Must have at least one non-header substantive line or OpenClaw skips heartbeat entirely.
   CRITICAL: If the grep command with absolute path pushes over 149, use relative path or remove the grep line (it's also in AGENTS.md).

5. Verify: `wc -c ~/.openclaw/workspace/HEARTBEAT.md` — must show under 149
6. Verify substantive content: `grep -c -v "^#\|^$" ~/.openclaw/workspace/HEARTBEAT.md` — must be >= 1

**After both files are trimmed, restart the gateway to activate changes:**
```bash
pkill -f "openclaw" && sleep 3 && nohup openclaw gateway > /dev/null 2>&1 &
sleep 5
ps aux | grep -v grep | grep openclaw
```

**Then verify no truncation warnings in fresh logs:**
Wait ~10 seconds, then:
```bash
# Find today's log
LOG=$(ls -t /tmp/openclaw-1000/openclaw-*.log 2>/dev/null | head -1)
# Check for truncation warnings
grep -i "chars (limit" "$LOG" | tail -5
```
If truncation warnings still appear for USER.md or HEARTBEAT.md, the files are still too large — trim more.
  </action>
  <verify>
On Server 101 via SSH:
1. `wc -c ~/.openclaw/workspace/USER.md` returns value under 2,520
2. `wc -c ~/.openclaw/workspace/HEARTBEAT.md` returns value under 149
3. `grep -c -E "Ground Rush|Sarah|Rowan|Rex|QUEUE|ESCALAT" ~/.openclaw/workspace/USER.md` returns >= 6
4. `grep -c -v "^#\|^$" ~/.openclaw/workspace/HEARTBEAT.md` returns >= 1
5. Fresh gateway logs show NO "chars (limit" warnings for USER.md or HEARTBEAT.md
  </verify>
  <done>USER.md is under 2,520 chars with key content preserved. HEARTBEAT.md is under 149 chars with at least one actionable line. Gateway restarted. No truncation WARNs in logs.</done>
</task>

</tasks>

<verification>
1. `wc -c ~/.openclaw/workspace/USER.md` < 2520
2. `wc -c ~/.openclaw/workspace/HEARTBEAT.md` < 149
3. Key content preserved in USER.md (family names, business, Queue/Escalation)
4. HEARTBEAT.md has substantive content (not empty/headers-only)
5. Post-restart gateway logs show zero truncation warnings for either file
</verification>

<success_criteria>
Both workspace files are within their actual OpenClaw runtime injection limits. Gateway logs show clean bootstrap and heartbeat cycles with no truncation WARNs for USER.md or HEARTBEAT.md.
</success_criteria>

<output>
After completion, create `.planning/phases/02-alfred-claw-config-fixes/02-04-SUMMARY.md`
</output>
