---
phase: 02-alfred-claw-config-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.openclaw/workspace/AGENTS.md
  - ~/.openclaw/workspace/SOUL.md
  - ~/.openclaw/workspace/TOOLS.md
  - ~/.openclaw/workspace/scripts/integrations/email_client.py
autonomous: true
requirements:
  - CLAW-01
  - CLAW-04
  - CLAW-05

must_haves:
  truths:
    - "AGENTS.md instructs the agent to reply HEARTBEAT_OK as plain text, not via the messaging tool"
    - "TOOLS.md shows Telegram target as numeric chat ID only (7582976864), not display name format"
    - "email_client.py dispatch handles the mark-read command without error"
    - "AGENTS.md grep command for QUEUE.md uses -E flag and absolute path"
  artifacts:
    - path: "~/.openclaw/workspace/AGENTS.md"
      provides: "Agent operating instructions with corrected heartbeat and grep behavior"
      contains: "HEARTBEAT_OK"
    - path: "~/.openclaw/workspace/SOUL.md"
      provides: "Agent identity with corrected Telegram send behavior"
      contains: "7582976864"
    - path: "~/.openclaw/workspace/TOOLS.md"
      provides: "Tool reference with correct argument formats"
      contains: "mark-read"
    - path: "~/.openclaw/workspace/scripts/integrations/email_client.py"
      provides: "Email tool with mark-read dispatch branch"
      contains: "mark-read"
  key_links:
    - from: "~/.openclaw/workspace/AGENTS.md"
      to: "OpenClaw heartbeat framework"
      via: "heartbeat behavior instructions"
      pattern: "HEARTBEAT_OK.*plain text"
    - from: "~/.openclaw/workspace/TOOLS.md"
      to: "~/.openclaw/workspace/scripts/integrations/email_client.py"
      via: "tool documentation matching dispatch"
      pattern: "mark-read"
---

<objective>
Fix Telegram duplicate messages, tool argument errors, and QUEUE.md grep syntax on Server 101 by updating the agent instruction files (AGENTS.md, SOUL.md, TOOLS.md) and patching the email_client.py dispatch.

Purpose: The Telegram dedup bug causes Mike to receive every heartbeat response twice. Tool argument errors cause CRM note creation, email mark-read, and Telegram sends to fail silently. The grep bug prevents the escalation bridge from finding pending items. These are the most user-visible bugs.

Output: Corrected workspace instruction files and patched email_client.py on Server 101.
</objective>

<execution_context>
@/home/aialfred/.claude/get-shit-done/workflows/execute-plan.md
@/home/aialfred/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-alfred-claw-config-fixes/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Telegram dedup and QUEUE.md grep in AGENTS.md and SOUL.md</name>
  <files>
~/.openclaw/workspace/AGENTS.md (on Server 101 via SSH)
~/.openclaw/workspace/SOUL.md (on Server 101 via SSH)
  </files>
  <action>
SSH to Server 101: `ssh -p 2222 brucewayne9@75.43.156.101`

**CLAW-01 Fix — Telegram duplicate messages:**

Root cause: The agent uses its messaging tool to explicitly send "HEARTBEAT_OK" to Telegram, then the heartbeat framework ALSO delivers the reply. Fix is behavioral.

1. Read current files:
   - `cat ~/.openclaw/workspace/AGENTS.md` (expect ~5,416 chars)
   - `cat ~/.openclaw/workspace/SOUL.md` (expect ~7,686 chars)

2. Back up both:
   - `cp ~/.openclaw/workspace/AGENTS.md ~/.openclaw/workspace/AGENTS.md.bak-$(date +%Y%m%d)`
   - `cp ~/.openclaw/workspace/SOUL.md ~/.openclaw/workspace/SOUL.md.bak-$(date +%Y%m%d)`

3. In AGENTS.md, find the heartbeat instructions section. Add or update to include this explicit instruction:
   ```
   ## Heartbeat Protocol
   During heartbeat runs, reply with HEARTBEAT_OK as your message text. Do NOT use the messaging tool (send-message/telegram) to deliver HEARTBEAT_OK — the heartbeat framework handles delivery automatically. Using the messaging tool causes duplicate messages.
   ```

4. In SOUL.md, find any Telegram send examples or messaging instructions. Ensure:
   - Telegram target format is documented as numeric chat ID only: `7582976864`
   - NOT the display name format: `MJ (@groundrushlabs) id:7582976864`
   - Add if not present: "When sending Telegram messages, use the numeric chat ID only (e.g., 7582976864), not display names."

**CLAW-04 Fix — QUEUE.md grep in AGENTS.md:**

5. In AGENTS.md, find any reference to grepping QUEUE.md. Update ALL occurrences:
   - FROM: `grep -i "PENDING|ESCALATED" QUEUE.md` (or similar without -E)
   - TO: `grep -E -i "PENDING|ESCALATED" ~/.openclaw/workspace/QUEUE.md`
   - The -E flag enables extended regex (pipe alternation). The absolute path ensures it works from any working directory.

6. Verify AGENTS.md stays under the TOOLS.md 20000-char limit awareness (AGENTS.md itself has no hard char limit from OpenClaw, but keep it reasonable — under 7,000 chars).
  </action>
  <verify>
`grep -c "HEARTBEAT_OK" ~/.openclaw/workspace/AGENTS.md` — finds the heartbeat protocol instruction.
`grep "messaging tool" ~/.openclaw/workspace/AGENTS.md` — confirms "Do NOT use the messaging tool" instruction exists.
`grep "7582976864" ~/.openclaw/workspace/SOUL.md` — confirms numeric chat ID is documented.
`grep -E "\-E" ~/.openclaw/workspace/AGENTS.md` — confirms grep -E flag is present in QUEUE.md check.
`grep "\.openclaw/workspace/QUEUE.md" ~/.openclaw/workspace/AGENTS.md` — confirms absolute path used.
  </verify>
  <done>AGENTS.md contains explicit "reply HEARTBEAT_OK as plain text, do NOT use messaging tool" instruction. SOUL.md documents Telegram target as numeric ID (7582976864). AGENTS.md grep for QUEUE.md uses -E flag and absolute path. Backups created.</done>
</task>

<task type="auto">
  <name>Task 2: Fix TOOLS.md and email_client.py tool argument errors</name>
  <files>
~/.openclaw/workspace/TOOLS.md (on Server 101 via SSH)
~/.openclaw/workspace/scripts/integrations/email_client.py (on Server 101 via SSH)
  </files>
  <action>
SSH to Server 101: `ssh -p 2222 brucewayne9@75.43.156.101`

**CLAW-05 Fix — Tool argument errors:**

1. Read current files:
   - `cat ~/.openclaw/workspace/TOOLS.md`
   - `cat ~/.openclaw/workspace/scripts/integrations/email_client.py`

2. Back up:
   - `cp ~/.openclaw/workspace/TOOLS.md ~/.openclaw/workspace/TOOLS.md.bak-$(date +%Y%m%d)`
   - `cp ~/.openclaw/workspace/scripts/integrations/email_client.py ~/.openclaw/workspace/scripts/integrations/email_client.py.bak-$(date +%Y%m%d)`

3. **Fix email_client.py mark-read dispatch (Bug 1):**
   Find the dispatch chain (elif chain near end of file). Add the missing mark-read branch between the `search` and `mark-unread` branches:
   ```python
   elif cmd == "mark-read" and len(sys.argv) > 3:
       print(json.dumps(mark_read(sys.argv[2], sys.argv[3]), indent=2))
   ```
   The `mark_read` function already exists (defined around line 233). Only the dispatch entry is missing.

4. **Fix TOOLS.md Telegram target format (Bug 2):**
   Find the Telegram send tool entry in TOOLS.md. Update the target parameter documentation:
   - FROM any display name format example
   - TO: `target: 7582976864` (numeric chat ID only)
   - Add note: "Target must be numeric chat ID. Do not use display names."

5. **Fix TOOLS.md email mark-read documentation:**
   Ensure mark-read is listed as a valid email_client.py command:
   ```
   python3 email_client.py mark-read <account> <message_id>
   ```
   Note: use `python3` not `python33` (a typo seen in logs).

6. **Verify TOOLS.md stays under 20,000 chars** (MEMORY.md notes limit is ~19,766 currently):
   `wc -c ~/.openclaw/workspace/TOOLS.md` — must stay under 20,000.

7. **CRM create-note format in TOOLS.md:**
   Verify TOOLS.md shows correct crm.py usage:
   ```
   python3 crm.py create-note <person_id> <note body text>
   python3 crm.py create-task '{"title":"...","assigneeId":"..."}'
   ```
   Update if format doesn't match actual crm.py dispatch.
  </action>
  <verify>
`python3 ~/.openclaw/workspace/scripts/integrations/email_client.py mark-read test_account test_id 2>&1 | head -5` — should NOT return "Unknown command" (may return auth error, which is fine — means dispatch reached the function).
`grep "mark-read" ~/.openclaw/workspace/scripts/integrations/email_client.py | wc -l` — should be 2+ (function definition + dispatch).
`grep "7582976864" ~/.openclaw/workspace/TOOLS.md` — numeric chat ID documented.
`grep "python33" ~/.openclaw/workspace/TOOLS.md` — should return 0 matches (no python33 typo).
`wc -c ~/.openclaw/workspace/TOOLS.md` — under 20,000 chars.
  </verify>
  <done>email_client.py dispatch handles mark-read command. TOOLS.md shows correct Telegram target format (numeric ID), correct email mark-read syntax (python3), and correct CRM argument formats. TOOLS.md stays under 20,000 chars. Backups created.</done>
</task>

</tasks>

<verification>
1. AGENTS.md heartbeat protocol: `grep -A2 "HEARTBEAT_OK" ~/.openclaw/workspace/AGENTS.md` shows "do NOT use messaging tool"
2. SOUL.md Telegram target: `grep "7582976864" ~/.openclaw/workspace/SOUL.md` matches
3. AGENTS.md grep fix: `grep "grep -E" ~/.openclaw/workspace/AGENTS.md` matches with absolute path
4. email_client.py: `grep "mark-read" ~/.openclaw/workspace/scripts/integrations/email_client.py` shows dispatch branch
5. TOOLS.md: no `python33`, has `7582976864`, under 20,000 chars
6. All backup files exist: `ls ~/.openclaw/workspace/*.bak-*`
</verification>

<success_criteria>
- Telegram dedup fix: AGENTS.md explicitly instructs plain-text HEARTBEAT_OK reply (no messaging tool)
- Telegram target: SOUL.md and TOOLS.md show numeric chat ID 7582976864
- QUEUE.md grep: AGENTS.md uses `grep -E -i` with absolute path
- email_client.py: mark-read command dispatches correctly
- TOOLS.md: correct argument formats, under 20,000 chars
- All modified files have dated backups
</success_criteria>

<output>
After completion, create `.planning/phases/02-alfred-claw-config-fixes/02-02-SUMMARY.md`
</output>
