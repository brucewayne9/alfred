---
phase: 02-alfred-claw-config-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.openclaw/workspace/USER.md
  - ~/.openclaw/workspace/HEARTBEAT.md
autonomous: true
requirements:
  - CLAW-02
  - CLAW-03

must_haves:
  truths:
    - "USER.md is under 3,955 chars and retains family details, business context, personality, key dates, and queue/escalation section"
    - "HEARTBEAT.md is under 293 chars, uses Service:STATUS(context) format with last-activity timestamps, and contains a functional checklist that will not be skipped by OpenClaw (not empty)"
  artifacts:
    - path: "~/.openclaw/workspace/USER.md"
      provides: "Mike's profile for Alfred Claw context injection"
      contains: "Ground Rush"
    - path: "~/.openclaw/workspace/HEARTBEAT.md"
      provides: "Heartbeat checklist for periodic health checks"
      contains: "HEARTBEAT_OK"
  key_links:
    - from: "~/.openclaw/workspace/USER.md"
      to: "OpenClaw bootstrap injection"
      via: "workspace file loader at session start"
      pattern: "USER.md"
    - from: "~/.openclaw/workspace/HEARTBEAT.md"
      to: "OpenClaw heartbeat framework"
      via: "heartbeat checklist injection"
      pattern: "HEARTBEAT.md"
---

<objective>
Trim USER.md and HEARTBEAT.md on Server 101 to fit within OpenClaw's hard character limits, stopping the WARN-level truncation that fires on every session bootstrap and heartbeat run.

Purpose: USER.md at 9,199 chars (limit 3,955) means Alfred only sees a truncated, incomplete profile. HEARTBEAT.md at 1,961 chars (limit 293) means heartbeat instructions are truncated and unreliable. Fixing these is the highest-impact change for Claw reliability.

Output: Two workspace files on Server 101, both within limits, preserving essential content per user decisions.
</objective>

<execution_context>
@/home/aialfred/.claude/get-shit-done/workflows/execute-plan.md
@/home/aialfred/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-alfred-claw-config-fixes/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trim USER.md to under 3,955 chars</name>
  <files>~/.openclaw/workspace/USER.md (on Server 101 via SSH)</files>
  <action>
SSH to Server 101: `ssh -p 2222 brucewayne9@75.43.156.101`

1. Read current USER.md: `cat ~/.openclaw/workspace/USER.md`
2. Count current chars: `wc -c ~/.openclaw/workspace/USER.md` (expect ~9,199)
3. Back up: `cp ~/.openclaw/workspace/USER.md ~/.openclaw/workspace/USER.md.bak-$(date +%Y%m%d)`
4. Rewrite USER.md applying these trimming rules (per user decision — keep both family AND business, trim equally):
   - **KEEP (compress)**: Identity + business overview, digital properties (1-line format), communication preferences (bullets), current clients (condensed), infrastructure (IP:role format), personal facts (name, birthday, origin), wife Sarah (facts only — name, birthday, kids), children Rowan + Rex (names, birthdays), key dates table, Queue/Escalation CRITICAL section
   - **CUT entirely**: "How Mike Operates — Ground Rush Evolution" narrative prose (~700 chars), "How we met" story for Sarah (~200 chars), grandchildren details for Brandon (~200 chars), parent/sibling addresses and locations (keep names + birthdays only), "Work Style" and "Things He Likes/Hates" placeholder sections (~200 chars), duplicate information across sections
   - **Format**: Bullet-point facts, no prose paragraphs. Compress infrastructure to `IP: role` single lines. Compress digital properties to `name: URL` single lines.
   - Target: ~3,800 chars (150 chars margin below 3,955 limit)
5. Verify: `wc -c ~/.openclaw/workspace/USER.md` — must be under 3,955
6. If over limit, identify remaining verbose sections and compress further

IMPORTANT: Preserve personality/communication preferences so Alfred matches Mike's style. Keep the Queue/Escalation section (marked CRITICAL in current file). Keep all birthday dates (Alfred uses these for reminders).
  </action>
  <verify>
`wc -c ~/.openclaw/workspace/USER.md` returns a value under 3,955. Content check: `grep -c "Ground Rush\|Sarah\|Rowan\|Rex\|QUEUE\|ESCALAT" ~/.openclaw/workspace/USER.md` returns 5+ matches (key content preserved).
  </verify>
  <done>USER.md is under 3,955 chars, retains family details (Sarah, Rowan, Rex, Brandon), business context (Ground Rush, clients), personality/communication preferences, key dates, and queue/escalation section. No truncation warning will fire at next bootstrap.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite HEARTBEAT.md to under 293 chars</name>
  <files>~/.openclaw/workspace/HEARTBEAT.md (on Server 101 via SSH)</files>
  <action>
SSH to Server 101: `ssh -p 2222 brucewayne9@75.43.156.101`

1. Back up: `cp ~/.openclaw/workspace/HEARTBEAT.md ~/.openclaw/workspace/HEARTBEAT.md.bak-$(date +%Y%m%d)`
2. Replace HEARTBEAT.md with this compact checklist (per user decision — local services only, condensed `Service:STATUS(context)` format with last-activity timestamps, and per research confirming the 293-char bootstrap limit):

```markdown
# Heartbeat
- Status: Ollama:OK/DOWN Gateway:OK/DOWN(since HH:MM) Telegram:OK/DOWN
- grep -E -i "PENDING|ESCALATED" ~/.openclaw/workspace/QUEUE.md
- Check birthdays within 7d
- Check alfred@ inbox
Reply HEARTBEAT_OK + status line.
```

This is approximately 250 chars — well within the 293-char limit. The status line uses the user's required `Service:STATUS(context)` format with last-activity timestamps (e.g., `Ollama:OK(3d) Gateway:DOWN(since 14:30)`). The grep command uses `-E` flag (fixes CLAW-04 requirement) and absolute path.

3. Verify char count: `wc -c ~/.openclaw/workspace/HEARTBEAT.md` — must be under 293
4. Verify not empty (OpenClaw skips heartbeat if HEARTBEAT.md is only headers/blank lines): `grep -c "^-" ~/.openclaw/workspace/HEARTBEAT.md` should return 4+ (checklist items)

IMPORTANT: The current 37-line detailed protocol document needs to become this 6-line micro-checklist. Protocol details that were in the old HEARTBEAT.md (if any are critical operational instructions) should be noted for potential inclusion in AGENTS.md or SOUL.md — but do NOT add them in this plan (Plan 02 handles those files).
  </action>
  <verify>
`wc -c ~/.openclaw/workspace/HEARTBEAT.md` returns a value under 293. `grep -E "PENDING|ESCALATED" ~/.openclaw/workspace/HEARTBEAT.md` matches (grep -E flag present). `grep -c "^-" ~/.openclaw/workspace/HEARTBEAT.md` returns 4 (checklist items exist, not empty).
  </verify>
  <done>HEARTBEAT.md is under 293 chars with a functional 4-item checklist including corrected grep -E syntax and absolute path. No truncation warning will fire. OpenClaw will not skip the heartbeat (file is not empty).</done>
</task>

</tasks>

<verification>
1. `wc -c ~/.openclaw/workspace/USER.md` — under 3,955
2. `wc -c ~/.openclaw/workspace/HEARTBEAT.md` — under 293
3. `grep -E "PENDING|ESCALATED" ~/.openclaw/workspace/HEARTBEAT.md` — matches (CLAW-04 grep fix present)
4. Key content spot checks on USER.md: Ground Rush, Sarah, Rowan, Rex, QUEUE references all present
5. Backup files exist: `ls ~/.openclaw/workspace/*.bak-*`
</verification>

<success_criteria>
- USER.md is under 3,955 chars with all essential content preserved per user decisions
- HEARTBEAT.md is under 293 chars with functional checklist
- Both files have backups created before modification
- The corrected `grep -E` command is embedded in HEARTBEAT.md (addresses CLAW-04)
</success_criteria>

<output>
After completion, create `.planning/phases/02-alfred-claw-config-fixes/02-01-SUMMARY.md`
</output>
