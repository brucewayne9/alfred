---
phase: 08-recovery-alerting
plan: 03
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - core/api/main.py
autonomous: true
requirements: [RECOV-01, RECOV-03]

must_haves:
  truths:
    - "Mike can ask Alfred 'did last night's backup succeed?' and get a meaningful answer"
    - "An API endpoint returns the latest backup status from data/backup_status.json"
    - "The response includes per-server daily/weekly status with age and file counts"
  artifacts:
    - path: "core/api/main.py"
      provides: "GET /api/backup/status endpoint returning backup validation results"
      contains: "/api/backup/status"
  key_links:
    - from: "core/api/main.py"
      to: "data/backup_status.json"
      via: "json.load reads the status file written by validate_backups.py"
      pattern: "backup_status\\.json"
---

<objective>
Add a backup status API endpoint so Alfred (both Labs UI and Claw) can answer questions about backup health.

Purpose: Success criterion #4 requires Mike to ask "did last night's backup succeed?" and get a meaningful answer. This endpoint provides the data backing that answer.
Output: GET /api/backup/status endpoint in main.py
</objective>

<execution_context>
@/home/aialfred/.claude/get-shit-done/workflows/execute-plan.md
@/home/aialfred/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-recovery-alerting/08-01-SUMMARY.md
@core/api/main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add GET /api/backup/status endpoint to Alfred Labs API</name>
  <files>core/api/main.py</files>
  <action>
Add a new endpoint to `core/api/main.py` in the admin/infrastructure section (near the circuit breaker endpoints or after the last admin endpoint). The endpoint should be:

```python
@app.get("/api/backup/status")
async def get_backup_status(user=Depends(require_auth)):
```

Implementation:
1. Read `data/backup_status.json` from the project data directory (use the same path pattern as other data files in main.py — check how `data/` paths are resolved)
2. If file doesn't exist, return `{"error": "No backup status available. Validation has not run yet.", "status": "unknown"}`
3. If file exists, load and return the JSON directly — it already has the right structure from validate_backups.py: `{timestamp, servers: {...}, summary: {total_checks, ok, stale, empty, missing}}`
4. Add a `human_summary` field to the response: a plain-English sentence like "All 7 servers backed up successfully (last check: 2026-02-27 05:00)" or "2 issues: alfred-claw daily backup is stale (48h old), lonewolf weekly backup is missing"
5. Require auth (Depends(require_auth)) but NOT admin — any authenticated user can check backup status
6. Handle file read errors gracefully (return 500 with error message, don't crash)

The `human_summary` field is what Alfred Claw will relay when Mike asks "did last night's backup succeed?" — it must be conversational and specific.
  </action>
  <verify>
1. `grep -n '/api/backup/status' core/api/main.py` shows the endpoint
2. `python3 -c "import ast; ast.parse(open('core/api/main.py').read()); print('syntax ok')"` passes
3. `curl -s http://localhost:8400/api/backup/status` returns JSON (may be auth-required or error if status file doesn't exist yet — both are valid)
  </verify>
  <done>
- GET /api/backup/status endpoint exists in main.py
- Returns backup_status.json contents with human_summary field
- Authenticated users can access it (not admin-only)
- Returns meaningful error when status file doesn't exist yet
- human_summary provides a conversational answer suitable for Alfred to relay
  </done>
</task>

</tasks>

<verification>
1. Endpoint exists and returns JSON
2. main.py parses without syntax errors
3. Response includes human_summary for conversational use
</verification>

<success_criteria>
- Mike can get backup status through Alfred Labs API
- human_summary is clear enough for Alfred Claw to relay as a natural language answer
- Endpoint is wired into the existing auth system
</success_criteria>

<output>
After completion, create `.planning/phases/08-recovery-alerting/08-03-SUMMARY.md`
</output>
