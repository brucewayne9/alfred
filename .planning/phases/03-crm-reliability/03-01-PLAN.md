---
phase: 03-crm-reliability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /home/brucewayne9/.openclaw/workspace/scripts/integrations/crm.py
  - /home/brucewayne9/.openclaw/workspace/TOOLS.md
autonomous: true
requirements:
  - CRM-01
  - CRM-02

must_haves:
  truths:
    - "Creating a note linked to a contact either fully succeeds (note + noteTarget) or fully rolls back (no orphaned note)"
    - "Creating a task linked to a contact either fully succeeds (task + taskTarget) or fully rolls back (no orphaned task)"
    - "Searching for a contact by name returns results from the full database, not just the first 50 records"
    - "Alfred Claw can invoke the new create-linked-note and create-linked-task commands via TOOLS.md"
    - "When search-people returns multiple contacts, output presents a numbered list for user disambiguation"
    - "When search-people returns zero contacts, output offers to create a new contact"
  artifacts:
    - path: "/home/brucewayne9/.openclaw/workspace/scripts/integrations/crm.py"
      provides: "create_linked_note() and create_linked_task() with rollback, search_people() with first:500"
      contains: "create_linked_note"
    - path: "/home/brucewayne9/.openclaw/workspace/TOOLS.md"
      provides: "CLI documentation for create-linked-note and create-linked-task commands"
      contains: "create-linked-note"
  key_links:
    - from: "crm.py create_linked_note()"
      to: "Twenty CRM REST API /rest/notes + /rest/noteTargets"
      via: "_request() calls with rollback DELETE on step-2 failure"
      pattern: "_request.*noteTargets.*POST"
    - from: "crm.py create_linked_task()"
      to: "Twenty CRM REST API /rest/tasks + /rest/taskTargets"
      via: "_request() calls with rollback DELETE on step-2 failure"
      pattern: "_request.*taskTargets.*POST"
    - from: "TOOLS.md"
      to: "crm.py CLI dispatch"
      via: "Command names matching dispatch in main()"
      pattern: "create-linked-note"
---

<objective>
Add atomic two-step linked note/task creation with rollback to crm.py, fix the broken create_note() function, increase contact search result cap from 50 to 500, and document new commands in TOOLS.md.

Purpose: CRM note/task creation currently cannot link records to contacts atomically (and the existing create_note is broken due to Twenty CRM's bodyV2 migration). Contact search truncates results at 50, missing contacts beyond that limit.

Output: Updated crm.py with create_linked_note(), create_linked_task(), and search cap fix; updated TOOLS.md with new command documentation.
</objective>

<execution_context>
@/home/aialfred/.claude/get-shit-done/workflows/execute-plan.md
@/home/aialfred/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-crm-reliability/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add create_linked_note() and create_linked_task() with rollback to crm.py</name>
  <files>/home/brucewayne9/.openclaw/workspace/scripts/integrations/crm.py</files>
  <action>
SSH to Server 101 (`ssh -p 2222 brucewayne9@75.43.156.101`). Read the current crm.py to understand existing structure, especially `_request()`, `create_note()`, `create_task()`, and the CLI dispatch in `main()`.

Add two new functions to crm.py:

**create_linked_note(person_id, body_markdown, title=""):**
1. Step 1: POST /rest/notes with `{"bodyV2": {"markdown": body_markdown}}` (add `"title": title` if provided). Extract `note_id` from `resp["data"]["createNote"]["id"]`.
2. Step 2: POST /rest/noteTargets with `{"noteId": note_id, "personId": person_id}`. Extract `target_id` from `resp["data"]["createNoteTarget"]["id"]`.
3. If step 1 fails: return error immediately (no cleanup needed).
4. If step 2 fails: DELETE /rest/notes/{note_id} (rollback), then return error message: "Note created but link to contact failed — note deleted (no orphan). Detail: {error}".
5. On success: return `{"success": True, "noteId": note_id, "targetId": target_id}`.

**create_linked_task(person_id, title, status="TODO"):**
1. Step 1: POST /rest/tasks with `{"title": title, "status": status}`. Extract `task_id` from `resp["data"]["createTask"]["id"]`.
2. Step 2: POST /rest/taskTargets with `{"taskId": task_id, "personId": person_id}`. Extract `target_id` from `resp["data"]["createTaskTarget"]["id"]`.
3. If step 1 fails: return error immediately.
4. If step 2 fails: DELETE /rest/tasks/{task_id} (rollback), then return error.
5. On success: return `{"success": True, "taskId": task_id, "targetId": target_id}`.

Add CLI dispatch entries in `main()` for:
- `create-linked-note`: args = person_id, body_markdown, optional title
- `create-linked-task`: args = person_id, title, optional status

Use the existing `_request()` helper for all API calls. Follow existing code style and error handling patterns. Use `scp` to transfer the edited file if shell escaping becomes complex (per Phase 2 established pattern).

Also fix the broken `create_note()` function: change the `body` field to `bodyV2: {"markdown": body}` format so the existing create-note command stops failing silently. Keep the old function but fix its API payload.
  </action>
  <verify>
SSH to 101 and run:
1. `python3 ~/.openclaw/workspace/scripts/integrations/crm.py create-linked-note --help` (or similar) confirms the command exists
2. `python3 ~/.openclaw/workspace/scripts/integrations/crm.py create-linked-note <test_person_id> "Test note from Phase 3"` — verify it returns success JSON with noteId and targetId
3. `python3 ~/.openclaw/workspace/scripts/integrations/crm.py create-linked-task <test_person_id> "Test task from Phase 3"` — verify it returns success JSON with taskId and targetId
4. **Rollback test (create-linked-note):** Call `create-linked-note` with a valid person_id. Capture the noteId from step 1 output. Then call `create-linked-note` using an invalid/nonexistent person_id (e.g., `00000000-0000-0000-0000-000000000000`) — confirm error output mentions rollback. Verify the note was deleted by running `curl` GET against `/rest/notes/{note_id}` and confirming 404 (no orphan).
5. **Rollback test (create-linked-task):** Same pattern — call `create-linked-task` with an invalid person_id, confirm error mentions rollback, confirm the task was deleted (GET returns 404).
6. Clean up any remaining test records: DELETE the successful test note and task via the API
  </verify>
  <done>
create_linked_note() and create_linked_task() exist in crm.py, are callable via CLI, and correctly perform two-step creation with rollback on step-2 failure. The old create_note() function uses bodyV2 format.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix search cap and update TOOLS.md with new commands</name>
  <files>
/home/brucewayne9/.openclaw/workspace/scripts/integrations/crm.py
/home/brucewayne9/.openclaw/workspace/TOOLS.md
  </files>
  <action>
SSH to Server 101.

**Fix search cap in crm.py:**
In the `search_people()` function, change `"first": 50` to `"first": 500`. No other changes needed — the function already uses server-side LIKE filtering.

If the response `count` equals 500, add a `"truncated": True` key to the return dict so the agent knows results were capped.

**Update TOOLS.md:**
Read current TOOLS.md to find the CRM section. Add entries for the two new commands:
- `create-linked-note <person_id> <body_markdown> [title]` — Creates a note and links it to a contact. Rolls back on link failure (no orphans). Returns noteId and targetId on success.
- `create-linked-task <person_id> <title> [status]` — Creates a task and links it to a contact. Status defaults to TODO. Rolls back on link failure. Returns taskId and targetId on success.

Also update the existing `search-people` entry to note that it now searches up to 500 results (not 50). Keep the TOOLS.md additions concise — each new command should be 1-2 lines max. Current TOOLS.md is 6,458 chars with 13,542 chars of headroom, so no risk of hitting the 20,000 limit.

After edits, restart the OpenClaw gateway so the updated TOOLS.md is loaded:
```
pkill -f openclaw; sleep 3; nohup openclaw gateway > /dev/null 2>&1 &; sleep 5; ps aux | grep openclaw
```
  </action>
  <verify>
1. SSH to 101 and run: `python3 ~/.openclaw/workspace/scripts/integrations/crm.py search-people "son"` — verify count is >50 (research showed 88 matches for "son", previously capped at 50)
2. `grep -c "first.*500" ~/.openclaw/workspace/scripts/integrations/crm.py` — confirms the cap change
3. `grep "create-linked-note" ~/.openclaw/workspace/TOOLS.md` — confirms TOOLS.md has the new command
4. `grep "create-linked-task" ~/.openclaw/workspace/TOOLS.md` — confirms TOOLS.md has the new command
5. `wc -c ~/.openclaw/workspace/TOOLS.md` — verify still under 20,000 chars
6. `ps aux | grep openclaw` — verify gateway is running with updated files
  </verify>
  <done>
search_people() returns up to 500 results instead of 50. TOOLS.md documents create-linked-note and create-linked-task commands. OpenClaw gateway restarted with updated workspace files.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add numbered-list disambiguation and zero-result create-offer to search-people output</name>
  <files>
/home/brucewayne9/.openclaw/workspace/scripts/integrations/crm.py
/home/brucewayne9/.openclaw/workspace/TOOLS.md
  </files>
  <action>
SSH to Server 101.

**Update search_people() output in crm.py:**

When `search_people()` returns results, change the output format based on result count:

1. **Multiple results (count > 1):** Print a numbered list to stdout so Alfred Claw can present it for user disambiguation. Format:
   ```
   Found {count} contacts matching "{query}":
   1. {first_name} {last_name} — {email} (id: {person_id})
   2. {first_name} {last_name} — {email} (id: {person_id})
   ...
   Reply with the number of the contact you want.
   ```
   Still return the full JSON data structure (the numbered list is the human-readable stdout output; the JSON is for programmatic use). If email is empty, omit the " — {email}" part.

2. **Single result (count == 1):** Keep current behavior — return the single contact directly. No disambiguation needed.

3. **Zero results (count == 0):** Print a helpful message:
   ```
   No contacts found matching "{query}". Would you like me to create a new contact?
   ```

This logic lives in the CLI output layer of `search_people()` (the `if __name__` / `main()` dispatch), NOT in the function's return value. The function still returns the raw data dict; the CLI formatter handles human-readable output.

**Update TOOLS.md:**
Update the `search-people` command description to note:
- Multiple matches: presents a numbered list for the user to pick from
- Zero matches: offers to create a new contact

Keep additions concise (1-2 lines). Verify `wc -c` stays under 20,000 chars.
  </action>
  <verify>
SSH to 101 and run:
1. `python3 ~/.openclaw/workspace/scripts/integrations/crm.py search-people "son"` — verify output shows a numbered list (count should be ~88, so expect "Found 88 contacts matching...")
2. `python3 ~/.openclaw/workspace/scripts/integrations/crm.py search-people "xyznonexistent123"` — verify output says "No contacts found" and offers to create
3. `python3 ~/.openclaw/workspace/scripts/integrations/crm.py search-people "Sarah"` — verify output shows numbered list if >1 result, or single contact if exactly 1
4. `grep "numbered list\|pick from\|create a new" ~/.openclaw/workspace/TOOLS.md` — confirms TOOLS.md documents the disambiguation behavior
5. `wc -c ~/.openclaw/workspace/TOOLS.md` — verify still under 20,000 chars
  </verify>
  <done>
search-people CLI output shows a numbered list when multiple contacts match (for user disambiguation) and offers to create a new contact when zero results are found. TOOLS.md documents both behaviors.
  </done>
</task>

</tasks>

<verification>
1. **CRM-01 (rollback):** Run `create-linked-note` and `create-linked-task` against a real contact — both succeed with noteId+targetId in response. To verify rollback: test with an invalid person_id — confirm error message mentions rollback and GET to the created note/task returns 404 (no orphan).
2. **CRM-02 (search cap):** Run `search-people "son"` and verify count > 50 (should be ~88). Run `search-people "Sarah"` and verify it returns expected contacts.
3. **CRM-02 (disambiguation):** Run `search-people "son"` and verify output is a numbered list with "Reply with the number" prompt. Run `search-people "xyznonexistent"` and verify output offers to create a new contact.
4. **TOOLS.md:** Verify `wc -c` is under 20,000 chars and new commands + disambiguation behavior documented.
5. **Gateway:** Verify OpenClaw process is running after restart.
</verification>

<success_criteria>
- create-linked-note creates a note + noteTarget atomically, rolls back on failure
- create-linked-task creates a task + taskTarget atomically, rolls back on failure
- search-people returns up to 500 results (not 50)
- search-people presents a numbered list when multiple contacts match
- search-people offers to create a new contact when zero results match
- TOOLS.md documents both new commands and disambiguation behavior
- OpenClaw gateway is running with updated workspace files
- No orphaned records left in Twenty CRM after testing
</success_criteria>

<output>
After completion, create `.planning/phases/03-crm-reliability/03-01-SUMMARY.md`
</output>
