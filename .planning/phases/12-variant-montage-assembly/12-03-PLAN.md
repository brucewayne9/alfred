---
phase: 12-variant-montage-assembly
plan: 03
type: execute
wave: 3
depends_on: ["12-02"]
files_modified:
  - core/forge/handlers.py
  - services/forge-web/index.html
  - tests/forge/test_topic_clip_handler.py
autonomous: false
requirements: [CLIP-02, CLIP-04]
user_setup: []

must_haves:
  truths:
    - "A topic_clip job runs the variant loop: each structural variant assembled, then multiply()'d for stealth uniqueness, then delivered"
    - "Finished variants appear in the Forge library alongside the other formats (delivered_dirs + variant_count populated)"
    - "Operator can push the topic_clip job's variants to Postiz as DRAFTS in one action (never auto-publish)"
    - "The Topic tab has an 'Assemble variants' button that POSTs source_id + curated segments + variant_count to /forge/jobs"
    - "An assembled variant from the real episode_5.mp3 source plays with synced A/V, is 9:16 branded, is ≤60s, and reaches a Postiz draft"
  artifacts:
    - path: "core/forge/handlers.py"
      provides: "_topic_clip_handler registered as 'topic_clip' — custom variant loop (not _run_remix_format/build_remixes)"
      contains: "topic_clip"
      min_lines: 0
    - path: "services/forge-web/index.html"
      provides: "Assemble variants button + variant count input on the Topic tab"
      contains: "assembleVariants"
      min_lines: 0
    - path: "tests/forge/test_topic_clip_handler.py"
      provides: "Handler test: enqueue -> result has delivered_dirs + variant_count + format=topic_clip"
      contains: "def test_"
      min_lines: 30
  key_links:
    - from: "services/forge-web/index.html"
      to: "POST /forge/jobs {job_type: topic_clip, params:{source_id, segments, variant_count, caption}}"
      via: "assembleVariants() reads #topicSource value + topicState.selected (kept) — source_id MUST be included (Phase-11 JSON omits it)"
      pattern: "topic_clip|assembleVariants"
    - from: "core/forge/handlers.py"
      to: "topic_clip.render per variant + multiply(master, n, allow_flip=False) + delivery.deliver"
      via: "_topic_clip_handler loops _build_variant_assemblies, renders each, multiplies, delivers"
      pattern: "register_handler\\(\"topic_clip\""
    - from: "core/forge/handlers.py result"
      to: "library.list_done_jobs + distribution.build_pack"
      via: "result dict emits delivered_dirs + variant_count + format so Library + Postiz work for free"
      pattern: "delivered_dirs|variant_count"
---

<objective>
Wire the topic_clip renderer into the live Forge pipeline (CLIP-04) and prove the whole phase end-to-end on the real `episode_5.mp3` source. This plan registers a CUSTOM `_topic_clip_handler` that loops the structural variants from plan 02 — rendering each, passing each through the existing `multiply()` for pixel-level anti-suppression (CLIP-02 Tier 2), and delivering via the existing chain so Library + Postiz drafts work for free. It adds the "Assemble variants" button to the Topic tab (carrying the source_id the Phase-11 JSON omits), then a blocking human-verify checkpoint confirms a real assembled variant plays synced, branded, ≤60s, and reaches a Postiz draft.

Purpose: CLIP-04 is mostly wiring — the delivery/library/Postiz chain is format-agnostic. The risk is the source_id gap (Phase-11 Copy-selection JSON has no source_id) and proving the audio-only path actually produces a playable branded vertical on the real RuckTalk interview, not just synthetic test fixtures.
Output: registered handler, Topic-tab assemble UI, handler test, and a verified real-source variant + Postiz draft.
</objective>

<execution_context>
@/home/aialfred/.claude/get-shit-done/workflows/execute-plan.md
@/home/aialfred/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/12-variant-montage-assembly/12-RESEARCH.md
@.planning/phases/12-variant-montage-assembly/12-02-SUMMARY.md

# Reuse — read before writing
@core/forge/handlers.py
@core/forge/multiply.py
@core/forge/delivery.py
@core/forge/library.py
@core/forge/distribution.py
@services/forge-web/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register custom _topic_clip_handler + handler test</name>
  <files>core/forge/handlers.py, tests/forge/test_topic_clip_handler.py</files>
  <action>
Add a CUSTOM handler to `core/forge/handlers.py` — do NOT route through `_run_remix_format` (it calls `build_remixes()` which varies vessel mood, wrong for structural segment variation per the research). Write `_topic_clip_handler(params: dict) -> dict` directly:

```
def _topic_clip_handler(params: dict) -> dict:
    import tempfile, time, shutil
    from pathlib import Path
    from core.forge.renderers.topic_clip import render, _build_variant_assemblies, enforce_duration
    from core.forge.multiply import multiply
    from core.forge import delivery
    segments = params.get("segments") or []
    variant_count = int(params.get("variant_count", 3) or 3)
    variant_count = max(1, min(10, variant_count))
    stealth = int(params.get("variations", 3) or 3)   # default 3 stealth copies per structural variant (research open Q3)
    base = (params.get("subfolder") or "Intelligent Clips").strip()
    stamp = int(time.time())
    variants = _build_variant_assemblies(enforce_duration(segments), variant_count)
    work = Path(tempfile.mkdtemp(prefix="forge_topic_clip_"))
    total_delivered, var_dirs = 0, []
    try:
        for i, var in enumerate(variants):
            forge_jobs.check_cancel()
            label = f"topic_clip_{stamp}_v{i:02d}"
            rp = dict(params); rp["variant_index"] = i; rp["segments"] = var["segments"]
            master = render(rp, work / f"{label}_master.mp4")
            forge_jobs.check_cancel()
            copies = multiply(master, stealth, work / f"v{i}", base_name=label, allow_flip=False) if stealth else []
            dest = f"{base}/{label}"
            delivered = 0
            try:
                delivery.deliver(master, dest, filename=master.name); delivered += 1
            except Exception: pass
            for c in copies:
                try: delivery.deliver(c, dest); delivered += 1
                except Exception: pass
            if delivered:
                var_dirs.append(f"Content/Mainstay-RodWave/{dest}"); total_delivered += delivered
    finally:
        shutil.rmtree(work, ignore_errors=True)
    return {"format": "topic_clip", "variant_count": len(variants),
            "variations_each": stealth, "delivered": total_delivered, "delivered_dirs": var_dirs}
```

Notes:
- `allow_flip=False` is mandatory (pitfall 5: captions/logo must not be mirrored).
- The result MUST emit `delivered_dirs`, `variant_count`, AND `variations_each` — `library.list_done_jobs` reads `variations_each` or falls back to `variant_count` (confirmed at line ~56), and `distribution.build_pack` reads `delivered_dirs`. This gets Library + Postiz for free.
- Register it: in `register_default_handlers()` add `forge_jobs.register_handler("topic_clip", _topic_clip_handler)`.

Create `tests/forge/test_topic_clip_handler.py` following `tests/forge/test_jobs.py` / `test_integration.py` style. Monkeypatch `topic_clip.render` to return a tiny stub file, monkeypatch `multiply` to return [], and monkeypatch `delivery.deliver` to a no-op that records calls (so the test needs no ffmpeg/ComfyUI/Nextcloud). Assert:
  - `_topic_clip_handler({"segments":[<3 segs summing 30s>], "variant_count":3})` returns a dict with `format=="topic_clip"`, `variant_count==3`, `delivered_dirs` length matching delivered variants.
  - With `variant_count=1`, exactly one variant rendered.
  - "topic_clip" is present in handler registry after `register_default_handlers()`.
  </action>
  <verify>python3 -m pytest tests/forge/test_topic_clip_handler.py -q && python3 -c "from core.forge import handlers, jobs; handlers.register_default_handlers(); print('topic_clip' in jobs._handlers if hasattr(jobs,'_handlers') else 'registered')"</verify>
  <done>"topic_clip" handler registered. Handler test passes with render/multiply/deliver monkeypatched. Result dict has format/variant_count/variations_each/delivered_dirs. allow_flip=False used. Full forge suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Topic-tab "Assemble variants" UI (carries source_id)</name>
  <files>services/forge-web/index.html</files>
  <action>
Add to the Topic tab in `services/forge-web/index.html`, next to the existing "Copy selection" button (near line ~441) and reusing the existing `topicState` curate state:

1. A variant-count number input: `<input id="topicVariantCount" type="number" value="3" min="1" max="10">` with a small label "Variants".
2. An "Assemble variants" button: `<button class="btn" type="button" onclick="assembleVariants()"><span class="k">⧉</span> Assemble variants</button>`.
3. JS function `assembleVariants()`:
   - Read `const sourceId = document.getElementById('topicSource').value;` — CRITICAL: the Phase-11 Copy-selection JSON omits source_id; the assemble POST MUST include it (research pitfall 4). If empty, `toast('Pick a source first')` and return.
   - Build the kept segment array EXACTLY like `copyTopicSelection()` does (start_s: trimIn, end_s: trimOut, text, speaker, score) from `topicState.selected` filtered on `kept`. If none, `toast('No segments selected')` return.
   - `const variantCount = parseInt(document.getElementById('topicVariantCount').value) || 3;`
   - `const caption = segments[0]?.text?.split('. ')[0]?.slice(0,120) || '';` (first sentence of first segment as the topic/caption seed — plain text, no escape chars per project rule).
   - POST to `/forge/jobs` with the existing fetch/auth pattern used by the other Forge submit buttons (find how leak_graphic/film_montage submit — same headers/credentials). Body: `{job_type:'topic_clip', params:{source_id: sourceId, segments, variant_count: variantCount, caption}}`.
   - On success toast `'Assembling ' + variantCount + ' variants — watch the queue'` and (if the app has a queue/jobs refresh helper) trigger it. On error toast the message.
   Follow the EXACT existing POST style (auth token / credentials:'include') already used by `pushPostiz()` and the other format submit buttons in this file — read those first so headers match.
  </action>
  <verify>grep -n "assembleVariants\|topicVariantCount\|topic_clip" services/forge-web/index.html</verify>
  <done>Topic tab shows a Variants count input + "Assemble variants" button. assembleVariants() reads #topicSource for source_id, builds the kept-segment array from topicState, and POSTs {job_type:'topic_clip', params:{source_id, segments, variant_count, caption}} using the same auth pattern as the other submit buttons. Caption is plain text (no \n escapes).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Real-source end-to-end checkpoint (episode_5.mp3 -> variant -> Postiz draft)</name>
  <action>
Before pausing for the human: restart the live service (`sudo systemctl restart forge-web.service`), confirm "topic_clip" is in the handler registry, and confirm the Topic tab loads. Then run an Assemble-variants job against the real episode_5.mp3 source yourself (variant_count=2, query "sleep and recovery") and confirm the job reaches "done" with populated delivered_dirs before handing off to the human to eyeball playback and create the Postiz draft. Do all automatable steps first; the checkpoint only covers the visual/playback judgement and the Postiz-draft confirmation that requires a human.
  </action>
  <what-built>
The full topic_clip pipeline wired live: Topic tab "Assemble variants" -> topic_clip job -> structural variants -> multiply stealth copies -> Nextcloud delivery -> Forge Library -> Postiz drafts. Claude has restarted the forge-web service (`sudo systemctl restart forge-web.service`) and confirmed the handler is registered.
  </what-built>
  <how-to-verify>
On the live gated Forge (https://aialfred.groundrushcloud.com/forge — mainstay / RodWave0619!):
1. Open the Topic tab. Pick the real source `episode_5.mp3` (source_id 7a0b98dcbd2e4ae9981661540a1dd335, the RuckTalk interview).
2. Query "sleep and recovery" (known to return 3 genuine on-topic segments at ~79%). Keep the matched segments.
3. Set Variants = 2. Click "Assemble variants".
4. Watch the render queue — the topic_clip job should run and complete.
5. Open the Library tab — confirm the topic_clip job appears with its variant count, and play at least one delivered variant. VERIFY: audio and video are in sync; it is a 9:16 vertical; the Mainstay logo is present; a caption is legible; total length is ≤60s.
6. On that job, click "Push to Postiz (drafts)". VERIFY: a DRAFT (not published) appears in Postiz.
Expected: at least one playable, synced, 9:16, branded, ≤60s variant from the real audio-only source, reachable as a Postiz draft. (Audio-only source => background is a ComfyUI-generated still, not black, unless ComfyUI was unavailable.)
  </how-to-verify>
  <resume-signal>Type "approved" if a real assembled variant plays synced + 9:16 + branded + ≤60s and a Postiz draft was created; otherwise describe what was wrong (sync drift, no logo, wrong aspect, over 60s, no caption, Postiz failed).</resume-signal>
</task>

</tasks>

<verification>
- `python3 -m pytest tests/forge/test_topic_clip_handler.py -q` passes
- `python3 -m pytest tests/forge -q` — no regressions
- "topic_clip" present in the handler registry after register_default_handlers()
- `grep -n "allow_flip=False" core/forge/handlers.py` confirms no mirroring of captioned clips
- `grep -n "source_id" services/forge-web/index.html` shows assembleVariants() includes source_id in the POST
- Checkpoint approved: real episode_5.mp3 variant plays synced, 9:16, branded, ≤60s, Postiz draft created
</verification>

<success_criteria>
- topic_clip job runs the variant loop and delivers via the existing chain (CLIP-04)
- Variants appear in the Forge Library and push to Postiz as drafts in one action (CLIP-04)
- Each variant passes through multiply(allow_flip=False) for stealth uniqueness (CLIP-02 Tier 2)
- The Assemble button carries source_id (closes the Phase-11 JSON gap)
- A real assembled variant from episode_5.mp3 is proven: synced, 9:16, branded, ≤60s, Postiz draft
</success_criteria>

<output>
After completion, create `.planning/phases/12-variant-montage-assembly/12-03-SUMMARY.md`
</output>
